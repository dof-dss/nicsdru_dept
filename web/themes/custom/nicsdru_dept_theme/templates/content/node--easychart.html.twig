{#
/**
 * @file
 * Theme override to display a feature node. This is a template
 * candidate for all view modes so may need splitting if the content type
 * uses further view modes in the future.
#}
{{ content }}
