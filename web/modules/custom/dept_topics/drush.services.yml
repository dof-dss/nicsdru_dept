services:
  dept_topics.commands:
    class: \Drupal\dept_topics\Commands\DeptTopicsCommands
    arguments: ['@entityqueue.repository', '@entity_type.manager', '@dept_migrate.migrate_uuid_lookup_manager']
    tags:
      - { name: drush.command }
